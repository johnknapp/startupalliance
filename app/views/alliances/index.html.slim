- content_for :page_title do
  ' Startup Alliance – Private Alliances maximizing success regardless of location

.row
  .medium-12.columns
    .callout.primary-background
      h1.bold.white-text.wide-line-height.text-center
        ' Private Alliances


    /h3.bold.primary-text Private Alliances
    /hr.little-snug-top
    .float-right
      p.snug-top.space-bottom Collaboration to maximize the success of each member and their companies
    p.snug-top.space-bottom
      = link_to new_alliance_path do
        i.fa.fa-plus
        | &nbsp;
        | &nbsp;
        ' Create your Private Alliance

    - if current_user and current_user.confirmed? and @alliances.present?
      table.hover.stacked
        thead
          tr
            th{width='25%'} Alliance name
            th{width='35%'} Mission statement
            th{width='10%'}
              .text-center Members
            th{width='10%'}
              .text-center Companies
            th{width='10%'}
              .text-center Recruiting

        tbody
          - @alliances.each do |alliance|
            tr
              td{valign='top'}= link_to alliance.name, alliance
              td{valign='top'}= alliance.mission
              td{valign='top' align='center'}= number_with_delimiter(alliance.members.count)
              td{valign='top' align='center'}= number_with_delimiter(alliance.member_companies.count)
              td{valign='top' align='center'}
                - if alliance.recruiting
                  ' Yes!
                - else
                  ' —
    - elsif current_user and !current_user.confirmed?
      = render 'partials/inactive_browse_roadblock'
    - else
      .row
        .medium-8.medium-offset-2.columns
          p.show-for-medium.snug-top &nbsp;
          p.little-snug-bottom.faint
            em
              ' (You must be a member to browse Alliances.)
          .callout.secondary
            h4.bold Connect with the Alliance that can help you most
            p
              ' Inspired by
              ' #{link_to 'Masterind Groups', 'https://en.wikipedia.org/wiki/Mastermind_group', target: '_blank'},
              ' a Private Alliance is a group of entrepreneurs who form partnerships to help each other become more successful.
              ' Private Alliances are provided with a set of collaboration tools to help members achieve goals, overcome challenges, expand networks and maximize success.
            p
              ' All Startup Alliance members can take part in any Private Alliance.

          p &nbsp;
          = render 'partials/join_form', acqsrc: 'alliance_index'
