/= javascript_include_tag 'tour_profile.js'
= render 'masthead'
p.snug-top.space-bottom.text-right
  ' Every member has a
  | TrueInsight Profile
  ' &trade;
  ' like this.
  ' Alliance and Company members even get a custom URL.

= render 'tabs'
